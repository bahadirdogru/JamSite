---
/**
 * Astro 5 i18n (prefixDefaultLocale: true) kök dizinde bir index.astro bekler.
 * Bu dosya sadece varsayılan dile yönlendirme yapmak içindir.
 */
import { defaultLang } from "../config/site.js";
return Astro.redirect(`/${defaultLang}/`);
---


